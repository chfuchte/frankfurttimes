---
import ArticlePreview from "../../../components/ArticlePreview.astro";
import DefaultLayout from "../../../layouts/DefaultLayout.astro";

let articles = Array<Object>();

try {
articles = await Astro.glob("../../../content/articles/satire/**/*.md");
} catch (e) {
  console.info("Keine Artikel für Seite 'Satire' gefunden");
};
---

<DefaultLayout pageTitle="Satire">
  {
    articles.map((article: any) => {
      return <ArticlePreview article={article} />
    })
  }

  {articles.length == 0 && <h1 class="error">Es wurden keine Artikel gefunden</h1>}
</DefaultLayout>

<style>
  .error {
    width: 100%;
    margin-top: 2em;
    text-align: center;
  }
</style>
