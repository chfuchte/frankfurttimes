---
import ArticlePreview from "../../../components/ArticlePreview.astro";
import DefaultLayout from "../../../layouts/DefaultLayout.astro";

let articles = Array<Object>();

try {
articles = await Astro.glob("../../../content/articles/inland/**/*.md")
} catch (e) {
  console.info("Keine Artikel für Seite 'Inland' gefunden");
};
---

<DefaultLayout pageTitle="Inland">
  {
    articles.map((article: any) => {
      <ArticlePreview article={article} />
    })
  }

  {articles.length == 0 && <h1 class="error">Es wurden keine Artikel gefunden</h1>}
</DefaultLayout>

<style>
  .error {
    width: 100%;
    margin-top: 2em;
    text-align: center;
  }
</style>
